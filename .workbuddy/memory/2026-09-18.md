# 2026-09-18

## 登录态网页访问（长截图 + 抓正文）—— Trellis 任务 09-18-logged-in-web-access

- 需求：让项目能带登录态访问网页。用户最初提"用 DrissionPage"，但 DrissionPage 是 Python 库，
  Java 项目要用得跨进程 + 多维护一个 Python 运行时；澄清后**不引入 Python**，
  改用 Playwright 已有的 `connectOverCDP` / `launchPersistentContext`（能力一一对应，且依赖已在 pom 里）。
- 走完整 Trellis 流程：建任务 → prd/design/implement + research → 用户确认 → start → 实现。
- 实现（3 个新类 + 1 个改造）：
  - `BrowserSessionProperties`（`@ConfigurationProperties("browser.session")`，cdp-endpoint / user-data-dir /
    profile-headless / max-text-chars，yml 里两个来源**默认注释掉** = 不启用登录态）
  - `PageSession`（AutoCloseable，三种 Mode：CDP / PROFILE / HEADLESS）
  - `BrowserSessionProvider`（按 CDP→profile→无头选模式；URL 校验收在它这里，两个工具共用）
  - `WebScreenshotTool` 改造：删掉自带的 launch，改用 provider；返回值加"来源：…"
  - `WebPageTextTool` 新增：`@Tool(name="fetch_page_text")`，标题 + innerText("body")，按 max-text-chars 截断
  - `@AiService(tools = {"webScreenshotTool", "webPageTextTool"})`
- 测试结果：WebScreenshotToolTest 4/4（向后兼容硬闸过），BrowserSessionProviderTest 4/4，WebPageTextToolTest 3/3。
- **阻塞项（环境问题，非代码）**：
  1. Docker Desktop 没启动 → jlra-pg 没起来 → 任何加载完整上下文的测试（含 LLM 集成测试）都失败在 JDBC。
  2. 真接管验证要用户自己重启 Chrome 带 `--remote-debugging-port=9222`（他的 Chrome 现在没开端口；
     profile 5.9G 且正在使用，复制不可靠）。

## 踩到的坑（Java / Spring）

- **record 的存取方法不能改返回类型**：想把 `Boolean profileHeadless` 的取值方法写成 `profileHeadless()` 返回 boolean
  会编译失败（"存取方法返回类型必须与记录组件类型匹配"）。改名为 `useHeadlessProfile()`。
- 用 `Boolean` 而不是 `boolean` 才能区分"yml 没写"和"显式写了 false"。
- `@SpringBootTest(classes=...)` 不要把 record 型的 `@ConfigurationProperties` 写进 classes，
  Spring 会当普通 bean 装配（找不到构造器参数）→ 必须走 `@EnableConfigurationProperties`。
- Playwright 两种模式的关闭语义不同：CDP 的 `browser.close()` 只断开连接（不会关掉用户浏览器），
  `launchPersistentContext` 的 `context.close()` 才会真关进程 —— 混用会误伤。
- CDP 模式不要调 `page.setViewportSize()`：会真的改用户窗口大小（走 CDP Emulation），用户看得见。

## 登录态最终跑通（关键结论）

- 用户给的验证页：`https://wx.zsxq.com/group/51121244585524`（知识星球）。
  未登录抓到 5 字（"笔记 榜单"，登录墙）；登录态抓到 2109 字 + 真实标题"拿个offer-开源&项目实战-马丁-知识星球"。
- **CDP 这条路走不通**：Chrome 151 在**默认** user-data-dir 下会直接忽略 `--remote-debugging-port`
  （进程起来了、端口不监听）。用户自己开的 9222 就是这个情况 —— 不是占用，是根本没监听。
  要走 CDP 必须自己指定非默认 `--user-data-dir`。
- **最终用 profile 模式**：复制日常 profile 到 `~/.config/google-chrome-jlra`，yml 配 `user-data-dir`。
- ❗ 复制 profile 最大的坑：只复制 `Cookies` 会拿到近乎空的旧库（1273 条 → 1 条），
  因为 Chrome 运行时新数据在 `Cookies-wal` 里。必须 `cp Cookies*`，并带上
  `Local State`（cookie 解密密钥）+ `Local Storage` / `IndexedDB`（token 常存在这里）。
- 副本是**快照**，会过期 → 写了 `scripts/sync-chrome-profile.sh` 一键同步（已在项目里，可反复跑）。
- 全量回归：15 个测试全绿（另有 1 个手动验证用例 @Disabled）。
- 测试教训：单元测试不要断言"未登录"这种依赖 yml 配置的具体模式，否则别人配了登录态就挂。

## 收官：STORED 存档注入 + 全量回归（同日续）

- 用户提"方案一：把 sessionStorage / localStorage / cookie 抄到磁盘再注入"，实现为第 4 种模式 `STORED`：
  - `LoginStateStore`：登录成功时 `page.context().storageState()` 存 cookie+localStorage 到
    `~/.config/JLRADemo/storage-state.json`，sessionStorage 单独存到同名 + `-session.json`
    （Playwright 的 storageState **不含** sessionStorage，只能自己提取、靠 `context.addInitScript` 写回）。
  - 优先级改为 CDP → STORED → PROFILE → HEADLESS。STORED 排在 PROFILE 前：存档是"登录那一刻的快照"，
    而 profile 副本里的 cookie 会过期/被判坏。
  - 存档命令：`mvn test -Dtest='LoggedInAccessVerificationTest#saveState'
    -Djunit.jupiter.conditions.deactivate=org.junit.*DisabledCondition`
- **实测结论（修正了之前的判断）**：知识星球登录态实际在 **cookie + localStorage，不在 sessionStorage**
  （存档里 sessionStorage 是 `{}`，恢复后照样登录）。四模式字数：无头=登录页 / profile+无头=登录页 /
  profile+有头=2109 字 / **CDP=10071 字 / STORED=10071 字**。sessionStorage 代码保留（对别的站点是唯一解）。
- 全量回归：`mvn -o clean test` → **18 通过 0 失败 4 跳过**（跳过的 4 个是手动验证用例 @Disabled）。
- 规格文档 `.trellis/spec/backend/browser-automation.md` 已更新实测数据表；Trellis 任务标记 completed。

## 两个隐蔽的构建坑（浪费了不少时间）

- **Javadoc 里多写一个 `*/` 会让 Maven "编译通过"**：`LoginStateStore` 类注释提前闭合，后几行落到注释外，
  javac 报"非法字符"却被增量编译跳过，直到 Spring 加载 bean 才抛 `Unresolved compilation problems`，
  表现为**所有**测试一起挂在 ApplicationContext 上。排查口诀：满屏 `Failed to load ApplicationContext`
  且带 `Unresolved compilation problems` → 先 `mvn clean compile` 看真实编译错误，别在 Spring 配置里找。
- 枚举 switch 会生成 `Xxx$1`（switch map），上一个坑导致它没生成 → 报
  `NoClassDefFoundError: BrowserSessionProvider$1`，容易误判成依赖问题。

## 提交到远程（OfferCraft）

- 远程：`https://github.com/MrliCST/OfferCraft.git`（分支 main，origin/main）。
- 本次两个提交：`6565285` fix(browser) 修注释闭合 + 测试构造参数 + 规格/任务更新；
  `10e3451` chore 补 .gitignore、把 target/ 移出版本控制（此前整个 target 都被跟踪）。
- **推送方式未定**：这台机器没有 GitHub 凭据 —— `~/.ssh/id_ed25519.pub` 是 Gitee 的 key
  （ssh -T git@github.com 直接 Permission denied），也没有 credential.helper / gh / .git-credentials。
  用户选择自己手动推。以后要在本机推 GitHub，要么加 GitHub 专用 SSH key，要么配 credential helper。
- ⚠️ 待办：`src/main/resources/application.yml` 里 postgres 密码是明文 `20260917`，已经进了仓库历史。
  建议改成 `${PG_PASSWORD:...}` 环境变量或挪到 `~/.config/JLRADemo/secret.yml`（跟 DeepSeek key 一样）。

## 项目约定

- 浏览器一律用系统 Chrome（channel=chrome），禁止让 Playwright 下载浏览器（跳过开关写在 BrowserConfig 的 setEnv 里）。
- 新增"要打开网页"的能力时，一律走 `BrowserSessionProvider`，不要自己 launch 浏览器。
- 登录态**首选存档注入**（`~/.config/JLRADemo/storage-state.json`，无头即可、不用养常开浏览器）；
  掉线（抓回来变登录页）就重跑一次 `saveState`。profile 副本 `~/.config/google-chrome-jlra` 是退路。
- 浏览器自动化的完整约定见 `.trellis/spec/backend/browser-automation.md`。

## 收官（同日续 2）：去掉 PROFILE 模式 + 全局策略 + 加 zhaopin 名单

- 用户指令："两个都去掉（PROFILE 目录模式 + 白名单外的全局策略），添加 https://i.zhaopin.com/resume/new 进配置名单，测试长截图。"
- 实测确认可以去掉：`PageSession.Mode.PROFILE`、`BrowserSessionProperties.userDataDir/profileHeadless/useHeadlessProfile/hasUserDataDir`、
  `BrowserSessionProvider.openWithProfile/expand`、`application.yml` 里的 `user-data-dir` / `storage-state-file` / `profile-headless` / `default-mode` 全部移除。
  现在策略只剩一条：**名单里的站用各自存档，名单外一律匿名无头**。没有"名单外也给登录态"的开关（安全要求）。
- record 配置类最终形状：`BrowserSessionProperties(String cdpEndpoint, int maxTextChars, List<SiteLogin> sites)` + 静态工厂 `of(cdp, maxTextChars)`。
  Spring `@ConfigurationProperties` record 仍只能用**单个规范构造器 + 静态工厂**，多构造器会触发 "No default constructor found"。
- `application.yml` 的 `browser.session.sites` 新增 `- host: i.zhaopin.com`，state-file 默认 `~/.config/JLRADemo/state/i.zhaopin.com.json`（该文件尚未存，需登录后跑 `mvn -o -q compile exec:java` 生成）。
- 全量回归：`mvn -o test` → **28 通过 0 失败 3 跳过**（长截图 WebScreenshotToolTest 4/4 全过，证明新配置下长截图可用）。
- 注意：`scripts/sync-chrome-profile.sh` 是 PROFILE 模式的遗留脚本，现在已无用，可删；`LoginStateStore.DEFAULT_STATE_FILE` 仅作 `expand` 的 null 兜底，无实际引用。
- 规格文档 `.trellis/spec/backend/browser-automation.md` 已同步去掉 PROFILE / global 描述，并补 i.zhaopin.com 名单示例。

## 智联爬取失败根因：两个 Chrome 实例互不打通（同日续 3）

- 用户实测：`i.zhaopin.com` 已在名单，但爬到的是未登录状态。根因不是代码 bug，而是**用户登录在 2 号 Chrome，代码（LoginStateCapture / CDP）连的是 1 号 Chrome（开 9222 端口那个）**。
- 关键认知：**多个 Chrome 实例的 cookie / 登录态完全隔离**，代码只连得到"启动 `--remote-debugging-port` 那个实例"。登录和调试端口必须在同一个浏览器里。
- 之前目录里没有任何 `zhaopin` 存档，也印证了：连到的是没登录的那个浏览器 → 没存出 zhaopin 存档 → 名单命中但 `store.exists()` 为 false → 降级匿名。
- 修法加固：`LoginStateCapture` 加了守卫——当带了 `onlyHost` 过滤却一个站点都没命中时，明确报错"连到的浏览器里没有任何包含该串的页面，常见原因是登录在另一个 Chrome 了"，并列出当前浏览器实际有的站点。编译通过（mvn -o -q compile EXIT=0）。
- 正确流程（一次性）：启动专用 Chrome 带 9222 端口 → **在这个窗口里**登录智联并停在 `i.zhaopin.com/resume/new` → 跑 `mvn -o -q compile exec:java -Dexec.mainClass=com.example.domain.browser.LoginStateCapture -Dexec.args="http://127.0.0.1:9222 ~/.config/JLRADemo/state zhaopin"` → 生成 `i.zhaopin.com.json` + `-session.json` → 重新爬即登录态。

### 实测补充（同日续 3 现场）
- 用户实际起的 9222 Chrome（PID 69522，14:21 启动）只带 `--remote-debugging-port=9222`、**没有** `--user-data-dir`。
- `ss -ltnp` + `curl http://127.0.0.1:9222/json/version` 确认 **9222 根本没监听**（默认 profile 静默忽略端口），`ps` 能看到进程但连不进。
- 所以"已经登录"也白搭——代码连的都是端口，端口没开就谈不上抓取。必须让用户**退出现有 Chrome 后用显式 `--user-data-dir`（同默认目录 `$HOME/.config/google-chrome` 可保登录）重启**，端口才会真监听。
- 排查口诀：`ps` 看到带调试端口的 chrome 但 `ss`/curl 连不上 → 九成是缺 `--user-data-dir`（默认目录忽略端口），先看 `/proc/<pid>/cmdline` 有没有 user-data-dir。

### 收官：智联爬取终打通（同日续 3 末）
- 根因链（四步）：① 首个 9222 Chrome 缺 `--user-data-dir` → 端口不监听；② 用户新开带端口的 Chrome 并登录 → 连通（`curl /json/version` 返回 Chrome/151）；③ `LoginStateCapture` 收 `~/...` 参数未展开 `~` → 存档落进项目内字面 `~/` 目录（**已修**：dir 改走 `LoginStateStore.expand`）；④ 抓到的主机是 www/xiaoyuan 而非 i.zhaopin.com（登录后跳到 www），但 cookie 含 `.zhaopin.com` 通配域 → 复制 www 存档为 `i.zhaopin.com.json` 即可对 i 生效。
- 端到端验证（无头 Chrome 灌存档真抓 `i.zhaopin.com/resume/new`，临时类 `ZhaopinVerify`/`ZhaopinVerifyTest` 跑完即删）：`TITLE: 我的简历`、`BODY_LEN:1030`、抓到真实简历正文（李永振/河北地质大学/计算机科学与技术/求职意向 Java、Python…）、`疑似登录墙? false` → **登录态爬取成功**。
- 修了 `LoginStateCapture` 的 `~` 展开 bug 并编译通过。state 目录最终留 `i.zhaopin.com.json`+`-session.json`（配置实际引用、已刷新为最新一份）、www/xiaoyuan 存档（备用）、wx.zsxq 存档。
- 注意：pom 里 `exec-maven-plugin` 的 `<configuration>` 写死了 `LoginStateCapture` 主类，`-Dexec.mainClass` 会被忽略 → 想跑别的 main 要么改 pom 要么走 surefire 测试。

### 限流三件套 + 推送（同日续 4）
- 用户要求给爬取加最小必要限流（防封号）：新增 `CrawlThrottle`（静态工具，无配置项）于 `src/main/java/com/example/domain/browser/`。
  - 限频：`beforeCrawl(url)` 里导航前 `ThreadLocalRandom.nextGaussian()` 高斯随机延迟（均值 1500ms / 标准差 700ms），避免固定间隔被风控识别。
  - 限流：每域名 `Semaphore(2)`，`beforeCrawl` acquireUninterruptibly / `afterCrawl` release，控制单域并行（默认 2）。
  - 限网：`blockUselessResources(Page)` 用 `page.route("**/*", ...)` 按 resourceType 拦截 image/font/stylesheet/media（必须在导航前调用）。
  - 接入：`BrowserSessionProvider.beginCrawl(page,url,automated)` 对自动化模式（STORED/HEADLESS）拦资源、对 CDP 不拦（避免干扰用户浏览器）；两个抓取工具在开页面前 `beforeCrawl`、关页面后 `afterCrawl`（finally 成对）。
  - 取舍：截图场景拦掉 stylesheet 会变成无样式长图；已在注释/提交信息说明，要美观就把 stylesheet 移出拦截名单。
  - 编译通过（`mvn -o -q compile` EXIT=0）；用户说"无需测试"，故未写单测。
- 推送：本机 GitHub 凭据现已可用（之前记录的"无凭据"已解决）。路由登录由用户自己提交 `fb20d37 增加多网站路由登录功能` 并已推送；限流提交 `06a18ee feat(browser): 爬取限流三件套（随机延迟 / 单域并发 / 资源拦截）` 已 `git push origin main` 成功（fb20d37..06a18ee）。
- ⚠️ 待修：`fb20d37` 把 `target/`（构建产物）也提交进版本控制了，覆盖掉 `10e3451` 的"停止跟踪"意图。建议 `git rm --cached -r target` 并确认 `.gitignore` 含 `target/` 后提交一次。

### 新方向：面试 agent 的"帖子转题库"清洗管道（设计讨论，同日 20:11）
- 用户要做的"面试 agent"第一步：把**购买的项目内部讨论网站**（关于 **RagentAI** 项目）的帖子，按发布时间整理成题库。
- 五栏目：RagentAI(核心文档)/技术问答/面试相关/优质面经/只看星主。
- 清洗管道设计：原始帖子 → 转 Markdown → Topic Guard(相关性过滤, 超轻量) → 按"栏目+星主角色+主帖/回复"规则路由 → 三套提取策略(纯星主主帖0.9 / 星主回复问答0.9 / 普通成员0.1且LLM补全答案) → 结构化 JSON(强制 published_at + authority_score + topic_key)。
- 关键依赖/待定（已向用户指出）：① 真实原始帖子结构(HTML? 栏目/作者角色/时间是否元数据)；② 爬取须保留整帖结构(主帖+全部回复+各自作者+时间)，否则 Rule2 星主回复无法识别；③ 技术栈未定(沿用 JLRADemo 的 Java+langchain4j 还是 Python+LangGraph)；④ 版本压制属检索层(同 topic_key 取最新/最高 authority)，提取层只注入 published_at/topic_key。
- 状态：设计讨论中，未动手。用户说要"根据实际爬取内容调整"。

### 真实爬取跑通 + 清洗管道设计草稿（同日 20:50 续）
- 写了 `ZsxqCrawler.java`（Playwright 无头 + `LoginStateStore` 存档登录态），按栏目各爬 5 篇，存 `crawl-output/zsxq/<key>.json`。用户首条要求"补充精华贴栏目"→ 已把 `精华` 加进 `COLUMNS`（共 6 栏：RagentAI/技术问答/面试相关/优质面经/精华/只看星主）。
- 知识星球结构确认：Angular SPA，**栏目=侧边栏 chip 客户端过滤、无独立 URL**；每帖 `<app-topic type="flow">` 可抽 作者 / 身份(`owner`=星主、`member`=星友) / 时间 / 正文(长帖点"展开全部") / 话题标签 / 回复(评论人+时间+内容) / 星主是否回复(回复 commenter 含"马丁"=Rule2 信号)。
- 两个爬取坑：① `blockUselessResources` 拦 stylesheet → chip 的 `innerText()` 返回空 → 点错栏目；去掉后改用 `textContent()`+重试修复。② `authorRole` 误判：`ng-star-inserted` 含 "star" 被当成星主 → 改成判 `owner`；且 Maven 增量编译用了旧 class，必须 `mvn clean compile` 才生效（后台 AKRpj2 在验证，带 DEBUG 打印 class）。
- 用户给了 `cankao.md`（7 类典型帖逐条标注要/不要），提炼关键结论：**栏目≠价值分层；马丁点赞≠进题库**。星主技术长文(六)=最高价值、保留图片、做系列串联；星主回复问答(一)=抽权威答案；避雷/纯上岸/无关技术分享(IDEA快捷键)/纯吐槽=丢弃。
- 产出设计草稿 `docs/zsxq-cleaning-pipeline-design.md`：帖子类型 5 类(Topic Guard) + 权威分(0.9/0.1) + 图片策略(仅星主技术文) + 系列串联/版本抑制 + Stage0-7 + 输出 schema + Q1-Q6 待老板拍板。
- 待办：AKRpj2 跑完确认 authorRole 修复 → 把 精华 纳入重新爬取(已加进 COLUMNS) → 最终 6×5 JSON 呈现用户。

### 库表设计定稿 + 两处增补（同日续 5，22:24）
- 技术选型已拍板并写入 `docs/zsxq-cleaning-pipeline-design.md` §11/§12：PG + pgvector / flexmark-html2md 做 HTML→MD / **Java 顺序管道不引入 LangGraph** / LLM 分类闸用 langchain4j `AiService`（确定性调用非 agentic）。
- 3 张表：`zsxq_raw_post`(血缘) / `zsxq_reply`(回复追溯) / `cleaned_doc`(题库+RAG，含 embedding vector)。检索只用 `cleaned_doc`：`ORDER BY embedding <=> :q` + WHERE 过滤。
- 用户提议并被接受的两处增补：① `cleaned_doc.question`（interview_qa 星友提问原文，非该类型 NULL）→ 支撑 question / question+answer 向量化弹性；② `cleaned_doc.raw_post_id` 显式外键 REFERENCES raw_post(post_id) → 直接 JOIN 溯源。
- 顺手修的坑：`raw_post.post_id` 原非 UNIQUE 但被 reply 外键引用（PG 非法 FK）→ 改 UNIQUE；`column` 是 SQL 保留字 → 改名 `src_column`。
- 可执行 DDL 落 `docs/zsxq-schema.sql`（vector(1536)，HNSW 索引；embedding 维度随嵌入模型改）。
- `member_post`(星友帖, 权威分 0.1) **已拍板：低权保留**（2026-09-18 22:43），检索 `authority_score >= :min` 默认 0.1 即命中，不丢弃。
- **图片处理（2026-09-18 22:55 改为 0 存储，已写入 §4）**：星主长文图全留；星主问答回复仅留流程图/架构图(qwen-vl 判 kind, 经 langchain4j)，表情包剥离；星友图全剥离。**不下载、不落本地文件**，只保留 `original_url`（知识星球图床长期可用）。对保留的图调 qwen-vl 生成文字概括 `description`，送嵌入模型向量化存入 `zsxq_image.embedding`，检索时图片独立召回（返回 description+URL，点 URL 看原图）。`zsxq_image` 由可选升级为**必需的第 4 张表**（post_id/original_url/kind/description/kept/embedding），schema.sql 已加。
- **当前状态：爬虫是纯文本，未采集任何图片 URL、也未下载图片**，且星主长文被截断到 ~260 字（未点"查看详情"）。图片 URL 采集+下载属全量阶段待办（§10）。
- 落地顺序：修爬虫截断(点"查看详情")+采集 img src+下载图片→flexmark 转 MD→建表→langchain4j AiService 分类闸→PG 入库+向量化→全量爬取(Q6 滚到 2026-02-04)。
