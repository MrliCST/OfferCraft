# 2026-09-19

## 重构：帖子转题库管道从 browser 包拆出（domain 解耦）

- 用户要求：帖子转题库管道单独一个 domain 文件夹，不能和 browser 掺和；browser 专管浏览器基建。
- 新建 `com.example.domain.zsxq` 包，把 4 个 zsxq 专属类从 `com.example.domain.browser` 迁出
  （`git mv` 保留历史）：
  - `ZsxqCrawler`（S0 爬虫）、`ZsxqCleaner`（S1–S7 清洗器）、`ZsxqExplore`（站点侦察）、`HtmlToMarkdown`（S1 归一化）。
- browser 包只留**通用浏览器基建**：`BrowserConfig` / `BrowserSessionProperties` / `BrowserSessionProvider` /
  `CrawlThrottle` / `LoginStateCapture` / `LoginStateStore` / `PageSession` / `SiteLoginRegistry`。
- 依赖方向：zsxq（上层 domain）单向依赖 browser 基建（`LoginStateStore`、`CrawlThrottle`）；
  browser 包不反向依赖管道。ZsxqCrawler 自己 `pw.chromium().launch` + `LoginStateStore` 存档，不依赖 `BrowserSessionProvider`。
- 改动点：4 个文件 package 声明改 `zsxq`；`ZsxqCrawler`/`ZsxqExplore` 补 `import com.example.domain.browser.{LoginStateStore,CrawlThrottle}`；
  `ZsxqCrawler` 删除原 `import ...browser.HtmlToMarkdown`（同包后无需 import）；`ZsxqCleaner` 仅改 package（同包简单名引用另两个 zsxq 类）。
- `mvn -o compile` 通过（EXIT=0）；全仓无 `com.example.domain.browser.{HtmlToMarkdown,Zsxq*}` 残留引用。
- 设计文档 `docs/zsxq-cleaning-pipeline-design.md` §6 补「代码组织」说明（包名 + 依赖方向）。
- 已 commit（待 push）。后续 S2+ 类也落在 zsxq 包。
- **已推送远程**：`git push origin main` 成功（d6baf3a..2206ce5），含 `3d7178a`(S1) 与 `2206ce5`(拆包)。本机 GitHub 凭据可用。

## S2 Topic Guard 分类闸（落地完成，已 commit 549e142，未推送）

- 用户指令：请你做S2。按设计文档 §2 把每篇帖子归到 6 类，合并 Q4 金句抽取。
- 新增 zsxq 包类：`TopicGuard`(接口) / `HeuristicTopicGuard`(@Component 正则兜底) /
  `LangchainTopicGuard`(@Primary，包裹 `TopicGuardAi` AiService) / `PostType`(枚举+权威分) /
  `Classification`(结果类) / `TopicGuardAi`(@AiService，DeepSeek 低温度) +
  单测 `TopicGuardTest`（9 用例全过）。
- `LangchainTopicGuard`：解析失败/越界 post_type → 回退 `HeuristicTopicGuard`（设计文档 §8 Q4）。
- `DeepSeekModelConfig` 新增 `deepseekClassifyModel` bean（temperature=0.1），供 `TopicGuardAi` 的
  `@AiService(chatModel="deepseekClassifyModel", streamingChatModel="deepseekStreamModel")` 装配。
- **langchain4j 1.20.0 API 已核实**（机器上有源码 `/home/lyz/code/langchain4j`）：
  `ChatModel`(原 `ChatLanguageModel`)、`ChatModel.doChat(ChatRequest)`(原 `generate`)、
  `AiServices.create(Class<T>, ChatModel)` 静态工厂 / `AiServices.builder(cls).chatModel(m).build()`、
  `AiMessage.from(String)` 仍存在、`ChatResponse.builder().aiMessage(..).build()`。
- `ZsxqCleaner` 抽掉内联正则，改消费 `Classification`；保留 S5 系列串联 / S6 版本抑制。
- `mvn -o test -Dtest=TopicGuardTest` → Tests run: 9, Failures: 0。
- 待办：询问用户是否推送；后续 S3–S7（落库/图片策略/系列串联/版本抑制/发射 + 全量爬取）。

## 管道进度盘点（用户问「清洗管道还有几步要走」，2026-09-19 09:45）

核查结论（对照 `docs/zsxq-cleaning-pipeline-design.md` §6 S0–S7 与实际代码）：

- **已完成**：S0 采集（但仍是每栏 5 篇 `DEFAULT_PER_COLUMN=5`，全量翻页 + 2026-02-04 日期窗未做）、
  S1 归一化、S2 分类闸、S5 系列串联、S6 版本抑制、Q2 资源聚合、Q4 金句（启发式 + LLM 均已通）。
- **S3 权威分**：`authority_score`/`star_master_verified`/`star_master_answer` 已在
  `ZsxqCleaningService.toDoc` + `Classification` 里算好，**但落库为 0**——
  全项目 grep 不到 `JdbcTemplate`/`EmbeddingModel`/`embedding`，四张表（已在
  `src/main/resources/db/schema.sql`）全空。
- **S4 图片策略**：只有 `keepImages` 布尔标记 + Crawler 已采集 imageUrls；
  kind 白名单判别（flowchart/architecture）与 `zsxq_image` 写入、多模态概括未做。
- **S7 发射**：只输出 JSON，未入库、未填 embedding。
- **环境事实**：DB 走 **JDBC**（`org.postgresql.Driver` + `jlra_demo`），不是 R2DBC；
  有 mybatis-plus-spring-boot3-starter（自带 spring-boot-starter-jdbc），JdbcTemplate 可用。
  无 EmbeddingModel 配置（百炼待接）。schema 里 `vector(1536)`，
  若用百炼 text-embedding-v3 是 **1024 维**，需改 schema。

## 端到端小样本验收（只看星主栏 3 条，2026-09-19 16:2x）

- 用户要求：先爬「只看星主」栏最近 3 条（2026-09-18 22:22 / 22:21 / 2026-09-16 23:28）验证链路。
- `ZsxqCrawler` 加了第三个参数「栏目 key 过滤」（逗号分隔，留空=全部）：
  `java ...ZsxqCrawler crawl-output/zsxq-smoke3 3 starmaster`（不覆盖真实产物 crawl-output/zsxq）。
- 结果：**3 条时间戳完全对上，S0 通过**；产出 `pachu.md` 给用户看。
- **发现两个一直存在但没暴露的 bug**（旧 30 篇样本里也有，说明「补齐 post_id/source_url」从未生效）：
  1. `app-topic` 元素上**没有 `topic-id` 属性**，feed 里的链接是 **`articles.zsxq.com/id_xxx.html`**
     （不是 `/topic/<id>`），所以 `postId` / `sourceUrl` 全是 null，
     `hasViewDetail` 那条「开详情页取全文」的兜底路径从未触发 → **正文停在 270 字预览**。
  2. 文章页需要登录态：无 cookie 打开会跳 `wx.zsxq.com/login`（curl 只能拿到 1876 字节 SPA 骨架）。
     所以 openDetail 必须复用带登录态的 BrowserContext。
- 修复方向（下一步）：从 `articles.zsxq.com/id_xxx` 提取文章 id 作 `postId`，并把它作为 `detailUrl`
  喂给 `openDetail`（带登录态 context）取全文 + 图片。

## 修复：星主长文全文 + postId（2026-09-19 16:3x，用户确认「详情就在 articles.zsxq.com 链接里」）

- **文章页 DOM（带登录态实测）**：`h1`=标题、`.content`=纯正文（12157 字符，feed 只有 270）、
  `.content img`=4 张正文图（页面共 16 张 img，其余是水印/头像/底部二维码 → **图片必须限定在正文容器内取**）。
  无 cookie 打开会跳 `wx.zsxq.com/login`。
- **ZsxqCrawler 改动**：
  1. 新常量 `ARTICLE_HOST = "articles.zsxq.com"` + `parseArticleId()`（`/id_xxx.html` → `id_xxx`）作稳定血缘键；
  2. `extractPost` 识别文章页链接 → `postId` / `sourceUrl`，且**文章页优先于 `/topic/` 详情页**；
  3. `openDetail` 选择器回退 `.talk-content-container .content` → `.content`，新增取 `h1` 标题，
     图片改从正文容器内取；`DetailResult` 增加 title 字段。
- **ZsxqCleaningService / ZsxqCleanedDoc 改动**：`docId` 由「栏目+序号」改为**优先 postId**
  （`zsxq-id_xxx`，重跑稳定可幂等入库）；`ZsxqCleanedDoc` 新增 `rawPostId` 血缘外键并填值。
- **验收结果（3 条重爬 + 重洗）**：正文 330 → **14313 / 24971 / 16350** 字，图片 0 → 4/11/4，
  postId 有值，3 篇全判 `tech_article`（权威 0.9），系列链 86w8tjbfds5f → x211s6c52mr5 → u32qjplonsiw。
  zsxq 测试 13 个全绿（WebPageTextToolTest 失败是无关的外部网页抓取测试）。
- 产物：`crawl-output/zsxq-smoke3/`（starmaster.json + question-bank.json + drops.json）、`pachu.md`（全文版 58KB）。

## 管道评估报告（全栏目 30 篇小样本，2026-09-19 17:0x）

- 新增 `zsxq/eval/`（按作用域独立，不进 model）：`PipelineMetrics` / `ColumnMetrics` / `MetricAnomaly`
  （DTO 独立成文件）+ `PipelineEvaluator`(@Service 纯计算) + `EvalReportRenderer`(@Service 渲染)
  + `ZsxqEvalRunner`(薄 CLI)，单测 `PipelineEvaluatorTest` 6 例全绿。
- 运行方式：`ZsxqCrawler crawl-output/zsxq-eval 5` → `ZsxqCleaner` → `ZsxqEvalRunner`
  → `docs/zsxq-pipeline-eval-report.md`。
- **报告结论：未通过**（血缘键缺失 23 篇 + 分类可疑 3 篇）。关键数据：
  - 只看星主 100% 有 postId（走文章页）；其余 5 栏几乎 0%（星友帖 **DOM 里没有任何 id 属性、0 个 a 链接**）。
  - 疑似截断 **0 篇**：星友提问帖本来就短（实测 24 字），所以截断判定改成
    「短 + 预览特征（省略号收尾 / 有文章页却没长正文）」，不能只看长度。
  - 丢弃 11/30（36.7%），全是无星主实质回答的面经吹水，符合 Q3。
- **重要发现：可用 API 拿官方 topic_id** ——
  `https://api.zsxq.com/v2/groups/51121244585524/topics?scope=all&count=20` 返回
  `{resp_data:{topics:[{topic_id, type, talk:{owner.name, text}, content_voice, create_time}]}}`；
  另有 `/v2/hashtags/<hid>/topics`（按栏目）、`/v2/groups/<gid>/topics/sticky`（置顶）。
  → 这是补 postId / sourceUrl 的正解，也是全量爬取可考虑的方向（正文在 talk.text，不用开详情页）。
- 顺手修：`ZsxqDropRecord` 缺 `@JsonCreator`，drops.json 从来没被读回来过（Jackson 造不出对象）。
- 评估器兜底关联：无 postId 时用「作者+时间+正文前30字」指纹；architecture_note 正文被 Q4 换成金句，
  再退一级用「作者+时间」。
- **待老板定**：星友提问帖被判 architecture_note 拿到 0.9 权威分（3 篇），
  权威分应加「作者身份护栏」（星友帖上限 0.1）。
- 栏目过滤**是生效的**（各栏作者完全不同）；interview/mianjing 前 4 篇相同是因为
  范特西同一批帖同时带两个标签，不是 chip 切换 bug。

## 知识星球 API 结构（实测，2026-09-19 17:1x）—— 方案 B 的依据

- **列表**：`GET https://api.zsxq.com/v2/groups/51121244585524/topics?scope=all&count=20`
  - 单条字段：`topic_id`(数字，官方血缘键)、`topic_uid`、`type`(talk/task)、
    `talk{owner{user_id,name}, text}`、`talk.article{title, article_id, article_url, inline_article_url}`、
    `title`、`content_voice`(语音帖)、`likes_count`、`comments_count`、`reading_count`、
    `digested`(精华)、`sticky`(置顶)、`create_time`(ISO 毫秒)。
  - **翻页**：`&end_time=<本页最后一条 create_time>`（URL-encode 冒号和 +），下一页会**重复上页最后一条**，需去重。
  - 用 Playwright 的 `context.request()` 发请求即可，自动带登录态 cookie。
- **评论**：`GET https://api.zsxq.com/v2/topics/<topic_id>/comments?count=20` → `resp_data.comments[]`（实测有效）。
- **关键限制**：`talk.text` 对**星主长文只是摘要**（实测 458 字，以 `...` 结尾），
  全文仍在 `talk.article.article_url` 指向的文章页（1.4 万字）。所以 API **不能替代**文章页取全文。
- **正文是富文本 `<e>` 标记**（不是 HTML）：`<e type="text_bold" title="%E3%80%8A..."/>`、
  `<e type="hashtag" hid=... />`、`<e type="web" href="URL-encoded"/>`、`<e type="img" ...>`（图片在正文里），
  需要写解析器（或转 HTML 后复用 HtmlToMarkdown）。
- 其他接口：`/v2/groups/<gid>/topics/sticky`（置顶）、`/v2/hashtags/<hid>/topics`（按栏目）、
  `/v2/groups/<gid>/topics/tasks`（作业）、`/v2/group/<gid>/files`。
